
{
  "name": "promptperfect",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Run index.html in a live server for development.'",
    "build": "echo 'No explicit build step for static files, Vercel will handle API routes.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^9.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}