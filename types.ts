
// No types currently needed for this simplified version.
// PromptTemplate interface removed as it's no longer used.

// If you add features that require shared complex types, define them here.
// For example:
// export interface UserProfile {
//   id: string;
//   name: string;
//   preferences: Record<string, any>;
// }
